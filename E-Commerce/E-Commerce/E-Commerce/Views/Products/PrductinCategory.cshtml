@model IEnumerable<E_Commerce.Models.Product>

@{
    ViewData["Title"] = "PrductinCategory";
}

@*<h1>Prduct in Category</h1>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Productname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Productdescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Productprice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Productimg)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Productname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Productdescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Productprice)
            </td>
            <td>
                <img src="@Url.Content(item.Productimg)" width="100">
            </td>
            <td>
      <a asp-action="AddtoCart" asp-controller="Products" asp-route-id="@item.ProductId" class="btn btn-success">Add To Cart</a>

            </td>
        </tr>
*@


            <div class="container-fluid pt-5">
                <div class="text-center mb-4">
                    <h2 class="section-title px-5"><span class="px-2">All Products</span></h2>
                </div>
                <div class="row px-xl-5 pb-3">
                    @foreach (var item in Model)
                    {
                        <div class="col-lg-4 col-md-6 col-sm-12 pb-1">
                            <div class="card product-item border-0 mb-4">
                                <div class="card-header product-img position-relative overflow-hidden bg-transparent border p-0">
                                    <img class="~/img-fluid w-100" src="@Url.Content(item.Productimg)" alt="" style="height:25rem; width:15rem;">
                                </div>
                                <div class="card-body border-left border-right text-center p-0 pt-4 pb-3">
                                    <h6 class="text-truncate mb-3">@Html.DisplayFor(modelItem => item.Productname)</h6>
                                    <div class="d-flex justify-content-center">
                                        <h6> @Html.DisplayFor(modelItem => item.Productprice) JD</h6>
                                    </div>
                                </div>
                                <div class="card-footer d-flex justify-content-between bg-light border">
                                    <a asp-action="ProductDetails" asp-controller="Products" asp-route-id="@item.ProductId" class="btn btn-sm text-dark p-0"><i class="fas fa-eye text-primary mr-1"></i>View Detail</a>
                                    
                                    <a asp-action="AddtoCart" asp-controller="Products" asp-route-id="@item.ProductId" class="btn btn-sm text-dark p-0"> <i class="fas fa-shopping-cart text-primary mr-1"></i>Add To Cart</a>

                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
<div style="text-align:center;"><a asp-action="Index" asp-controller="Home" class="btn btn-primary">Back </a></div>







   
